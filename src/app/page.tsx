const HomePage = () => {
  return (
    <div>
      Get ?url=xxx&url=yyy&url=zzz and render a form with these openapis
      available to be selected from. This form submits to /new which makes the
      proxy.
    </div>
  );
};

export default HomePage;
